# Architecture
TARGET_ARCH := arm64
TARGET_ARCH_VARIANT := armv8-a
TARGET_CPU_ABI := arm64-v8a
TARGET_CPU_VARIANT := generic

# Kernel / Boot
BOARD_BOOTIMG_HEADER_VERSION := 4
BOARD_MKBOOTIMG_ARGS += --header_version $(BOARD_BOOTIMG_HEADER_VERSION)

# Dynamic Partitions & File Systems
BOARD_SUPER_PARTITION_GROUPS := main
BOARD_MAIN_PARTITION_LIST := system system_ext vendor product odm
BOARD_FLASH_BLOCK_SIZE := 131072

# Crypto / Decryption (Wajib untuk FBE v2 lo tadi)
BOARD_USES_METADATA_ENCRYPTION := true
PLATFORM_VERSION := 12 # Sesuaikan dengan versi Android stock HP lo (11/12/13)
PLATFORM_SECURITY_PATCH := 2026-01-01 # Sesuaikan dengan tanggal patch keamanan di tentang telepon
